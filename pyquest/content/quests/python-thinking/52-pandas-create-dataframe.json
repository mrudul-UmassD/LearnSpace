{
  "id": "52-pandas-create-dataframe",
  "world": "python-thinking",
  "title": "Pandas: Creating DataFrames",
  "story": "DataFrames are the core data structure in Pandas. Learn to create them from dictionaries—the most common way to build DataFrames from scratch.",
  "instructions": "Create a DataFrame from a dictionary with columns 'name', 'age', and 'city'. Use the data: names=['Alice', 'Bob', 'Charlie'], ages=[25, 30, 35], cities=['NYC', 'LA', 'Chicago']. Print the DataFrame.",
  "type": "code",
  "starterCode": "import pandas as pd\n\n# Create DataFrame from dictionary\ndata = {\n    'name': ['Alice', 'Bob', 'Charlie'],\n    'age': [25, 30, 35],\n    'city': ['NYC', 'LA', 'Chicago']\n}\ndf = pd.DataFrame(data)\nprint(df)",
  "solutionHidden": "import pandas as pd\n\ndata = {\n    'name': ['Alice', 'Bob', 'Charlie'],\n    'age': [25, 30, 35],\n    'city': ['NYC', 'LA', 'Chicago']\n}\ndf = pd.DataFrame(data)\nprint(df)",
  "tests": [
    {
      "id": "test1",
      "type": "output",
      "description": "Should create and print DataFrame with 3 rows",
      "expectedBehavior": "Create DataFrame with name, age, city columns",
      "expected": "      name  age     city\n0    Alice   25      NYC\n1      Bob   30       LA\n2  Charlie   35  Chicago"
    }
  ],
  "hints": [
    {
      "level": 1,
      "text": "Use pd.DataFrame() with a dictionary where keys are column names and values are lists."
    },
    {
      "level": 2,
      "text": "Dictionary format: {'col1': [val1, val2], 'col2': [val3, val4]}"
    }
  ],
  "hintUnlockAttempts": 2,
  "xpReward": 20,
  "difficulty": "beginner",
  "order": 52
}
